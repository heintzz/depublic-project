import RouterConfig from "utils/router";
import SmoothScroll from "./components/SmoothScroll";
const App = () => {
  return (
    <SmoothScroll>
      <RouterConfig />
    </SmoothScroll>
  );
};
export default App;
